import CardModel from "../model/card-model";
export default class FlashCardPlayer {
    cardModel: CardModel;
    constructor();
    initItem(uid: string, itemJSON: any, container: any, initOptions: any): void;
}
