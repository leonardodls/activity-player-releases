/// <reference types="react" />
interface Props {
    type: "video" | "image" | "audio";
    data: string;
    padding?: string | number;
    maxWidth?: string | number;
    margin?: string | number;
}
declare function Media(props: Props): JSX.Element;
declare namespace Media {
    var defaultProps: {
        maxWidth: string;
        margin: string;
    };
}
export default Media;
