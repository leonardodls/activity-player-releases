import * as React from "react";
interface Props {
    generalFeedback: string;
    feedback: string;
    isCorrect: boolean;
    labels?: {
        inCorrectTitle?: React.ReactNode;
        correctTitle?: React.ReactNode;
    };
    backgroundColor?: string;
}
declare const Feedback: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLDivElement>>;
export default Feedback;
