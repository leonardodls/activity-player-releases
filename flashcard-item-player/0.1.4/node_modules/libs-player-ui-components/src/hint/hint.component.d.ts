import * as React from "react";
import { Color } from "../utils/theme_helpers";
interface Props {
    labels?: {
        title?: React.ReactNode;
    };
    hintText: string;
    backgroundColor?: Color;
}
declare const Hint: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLDivElement>>;
export default Hint;
