import { Color } from "../utils/theme_helpers";
interface DividerProps {
    borderColor?: Color;
}
declare const Divider: import("styled-components").StyledComponent<"div", any, DividerProps, never>;
export default Divider;
